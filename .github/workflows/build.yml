name: Build Kernel
on:
  workflow_dispatch:
  workflow_call:

jobs:
  Build-Kernel:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - name: Build Kernel
      uses: tiktoetoe/kernel_build_action@main
      with:
          kernel-url: https://github.com/samsungexynos9810/kernel_samsung_exynos9810-V2
          kernel-dir: msm-4.9
          branch: everest-up1a
          config: exynos9810-starlte_defconfig
          arch: arm64
          aosp-gcc: true
          aosp-clang: true
          ksu: true
          ksu-version: main
          android-version: 13
          aosp-clang-version: r450784d
          python-27: true
          disable-lto: true
          anykernel3: true
